print: (inp: u32);
printf: (format: string, ...);

test :=(i: int, ...) -> int{
    return i+1;
}

/*
This is a comment man, don't worry
*/

/* You know what would be cool?
/* nested comments!
*/ C would not understand this, hahaha!*/

struct test_s {
  id: u32;
}

// Line comments work too, aint it cool?
main := (argc: u32, argv: []string) -> int{

    s1: test_s;

    s1.id = 3222; // TODO this isn't working!

    printf("My struct id is %d\n", s1.id);

    ans := test(41, 3);

    loopCount := 0;

   if(ans == 42){
     while ans {

         ans = ans - 1;
         loopCount = loopCount + 1;
     }
   }
    ans = test(3);

     while ans{
         ans = ans - 1;
         loopCount = loopCount + 1;
         print(loopCount);
     }


    return 0;
}
